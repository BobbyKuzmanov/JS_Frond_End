function solve(input){
  let first = input[0];
  let last = input[input.length - 1];
  console.log(first + last);
}

solve([20, 30, 40])

// Write a function that receives an array of numbers and prints the sum of
//  the first and last element in that array. 