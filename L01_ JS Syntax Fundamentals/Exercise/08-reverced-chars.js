function solve(a, b, c) {
  console.log(c + " " + b + " " + a);
}

solve("A", "B", "C");

// Write a program that takes 3 parameters (characters) and
//  prints them in reversed order with a space between them.
